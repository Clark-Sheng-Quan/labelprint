services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: label_printer
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5433:5432"
    volumes:
      - postgres_label_data:/var/lib/postgresql/data
    networks:
      - labelprint-network

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "3002:3002"
    depends_on:
      - postgres
    networks:
      - labelprint-network
    volumes:
      - ./src:/app/src
    command: npm run dev

volumes:
  postgres_label_data:
    driver: local

networks:
  labelprint-network:
    driver: bridge

